---
description: Static helper classes (non-DB only)
globs:
  - packages/nest/**
  - packages/shared/**
alwaysApply: false
---

# Static helpers

Use static classes to group **non-persistence** helpers (path/key builders, PIN hash, tus metadata parsers, formatters).

Do **not** use static classes as a DB access / repository layer — Drizzle lives in use-cases.
